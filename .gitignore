# --- Node / pnpm -----------------------------------------------------------
node_modules/
.pnpm-store/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Build outputs ---------------------------------------------------------
# `dist/per-view/` holds Chrome user-data caches generated by
# `scripts/capture-views.mjs` (~60 MB) — build artifact, not a deliverable.
# The committed PDF (`dist/RegulonDB-MG-prototype.pdf`) is kept for now;
# move it out of `dist/` if we want it versioned independently.
dist/per-view/

# Vite / Storybook build outputs
lib/dist/
lib/storybook-static/
.vite/
.cache/

# --- Editor / OS -----------------------------------------------------------
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# --- Env / secrets ---------------------------------------------------------
.env
.env.*
!.env.example
